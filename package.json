{
  "name": "stadium-app",
  "version": "1.0.0",
  "description": "Smart Stadium Prototype",
  "main": "app.js",
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
